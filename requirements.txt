# Use PyTorch CUDA wheels in addition to PyPI
--extra-index-url https://download.pytorch.org/whl/cu121

# Validated for Python 3.11.6

torch==2.5.0
torchvision==0.20.0
torchaudio==2.5.0

# Everything below comes from the default PyPI index
diffusers==0.31.0
transformers==4.43.2
accelerate==1.0.1
einops==0.8.0
roma==1.5.1
open3d==0.18.0
scikit-learn==1.5.2
pyrallis==0.3.1
jaxtyping==0.2.34
opencv-python==4.10.0.84
matplotlib==3.9.2
regex==2023.10.3
tokenizers==0.19.1

# HuggingFace Hub + CLI + missing runtime dependencies
huggingface_hub[cli]==0.26.2

# Runtime HTTP + progress bar dependencies (HF Hub does NOT declare these)
requests==2.32.3
urllib3==2.2.2
idna==3.7
certifi==2024.8.30
charset-normalizer==3.3.2
tqdm==4.66.4
pyyaml==6.0.2

# pyrallis missing runtime dependencies
typing_inspect==0.9.0
mypy_extensions==1.0.0

# diffusers missing runtime dependency
safetensors==0.4.3

# Splatting (installed separately with --no-build-isolation)
git+https://github.com/pesser/splatting
